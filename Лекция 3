 Функции.
# Чтобы создать функцию используем ключевое слово def.

def factorial(number):  # после def идет название функции и в скобках (используемые аргументы).
    result = 1
    for n in range(1, number + 1):
        result = result * n
    return result # завершается фцнкция ключевым слово return и объектом, оторый нужно вернуть
    # Если сохранить результат работы фунции в переменнную, то в ней будет то, что возвращает функция
    # Ели не указать команду return, то функция вернет объект типа None

print(factorial(10))
